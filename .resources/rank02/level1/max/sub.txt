Assignment name  : max
Expected files   : max.c
Allowed functions:
--------------------------------------------------------------------------------

以下の関数を作成してください:

int		max(int* tab, unsigned int len);

最初のパラメータはintの配列で、2番目のパラメータは配列内の要素数です。

関数は配列内で見つかった最大の数値を返します。

配列が空の場合、関数は0を返します。
